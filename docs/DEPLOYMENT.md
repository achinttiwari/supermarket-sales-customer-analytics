# Deployment

The app targets Python 3.10+ and can be deployed to Streamlit hosting or another compatible Python environment.

Before deployment:

1. Supply the real dataset through an approved data-loading mechanism.
2. Install requirements.
3. Run Ruff and pytest.
4. Start the application with streamlit run app.py.

The source CSV is intentionally not committed because it was not supplied with the project specification.
