# Raw dataset

Place the supplied `customer_shopping_behavior.csv` file in this directory.

The repository code expects this path for local cleaning, dashboard execution, and CI. The dataset is not committed by default; keep the source file local when redistribution rights are not established.
